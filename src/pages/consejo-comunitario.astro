---
import { getEntry } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import OrgPage from '../components/OrgPage.astro';

const entry = await getEntry('organizaciones', 'consejo-comunitario');
if (!entry) throw new Error('Entrada "consejo-comunitario" no encontrada en la colección organizaciones');
---

<BaseLayout title={entry.data.title} description={entry.data.tagline}>
	<OrgPage entry={entry} />
</BaseLayout>
